# 🤖 Гід по автоматизації деплою та тестування

## ✅ Що вже налаштовано:

1. **GitHub репозиторій**: https://github.com/kulishdenis-Tech/fxhub_backend
2. **render.yaml**: Готовий для автоматичного деплою
3. **Автоматичний деплой при push**: Render автоматично деплоїть при кожному `git push`

## 🚀 Як працює автоматизація:

### Після першого деплою (через веб або API):

Я можу автоматично:

1. **Тестувати production API:**
   ```bash
   python test_production.py
   ```
   - Тестує всі ендпоінти
   - Перевіряє відповіді
   - Виявляє помилки

2. **Виправляти помилки:**
   - Роблю зміни в коді
   - Тестую локально

3. **Автоматичний деплой:**
   ```bash
   python auto_fix_and_deploy.py
   ```
   - Commit змін
   - Push на GitHub
   - Render автоматично передеплоїть
   - Тестування після деплою

## 📝 Робочий процес:

```
1. Виявлення проблеми (тестами або логами)
   ↓
2. Виправлення в коді
   ↓
3. Тестування локально
   ↓
4. git commit && git push
   ↓
5. Render автоматично деплоїть (1-2 хв)
   ↓
6. Автоматичне тестування production
   ↓
7. Якщо OK → готово
   Якщо помилка → повертаємось до кроку 2
```

## 🔧 Скрипти автоматизації:

### `test_production.py`
Тестує всі ендпоінти на production:
- `/` - root
- `/exchangers/list` - список обмінників
- `/currencies/list` - список валют
- `/rates/bestrate` - найкращі курси

**Використання:**
```bash
python test_production.py
```

### `auto_fix_and_deploy.py`
Повний цикл: тест → commit → push → очікування деплою → повторний тест

**Використання:**
```bash
python auto_fix_and_deploy.py
```

## ⚙️ Налаштування:

### Environment Variables в `.env`:

```env
# Render URL (після деплою)
RENDER_URL=https://fxhub-backend.onrender.com

# Supabase (вже налаштовано)
SUPABASE_URL=...
SUPABASE_KEY=...

# GitHub (вже налаштовано)
GITHUB_TOKEN=...
```

## 🎯 Приклад використання:

### Сценарій 1: Тестування після деплою
```bash
python test_production.py
```

### Сценарій 2: Виправлення та деплой
1. Виправляю помилку в коді
2. ```bash
   python auto_fix_and_deploy.py
   ```
3. Чекаю 1-2 хвилини
4. Перевіряю результат

### Сценарій 3: Ручне оновлення
```bash
git add .
git commit -m "Fix: опис виправлення"
git push
# Render автоматично деплоїть
```

## 📊 Моніторинг:

### Перевірка статусу деплою:
- Render Dashboard: https://dashboard.render.com
- GitHub Actions (якщо налаштовано)
- Логи: `test_production.py` показує статус

### Логи Render:
1. Відкрий Render Dashboard
2. Вибери сервіс `fxhub-backend`
3. Вкладка "Logs"

## ✅ Переваги:

- ✅ Автоматичний деплой при push
- ✅ Автоматичне тестування
- ✅ Швидкий цикл розробки
- ✅ Всі зміни версіоновані в Git
- ✅ Можливість rollback (git revert)

## 🚨 Важливо:

1. **Перший деплой**: Через Render Dashboard (1 раз)
2. **Надалі**: Автоматично при `git push`
3. **Тестування**: Завжди перед push
4. **Environment Variables**: Налаштовані в Render Dashboard

---

## 💡 Як я можу допомогти:

Після налаштування я можу:
- ✅ Тестувати API автоматично
- ✅ Виправляти помилки
- ✅ Деплоїти зміни
- ✅ Моніторити статус
- ✅ Оптимізувати код

Просто скажи що потрібно виправити або протестувати!
